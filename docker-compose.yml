services:
  invitacion-boda:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: invitacion-boda
    restart: unless-stopped
    ports:
      - "${APP_BIND:-0.0.0.0}:${APP_PORT:-8085}:80"
